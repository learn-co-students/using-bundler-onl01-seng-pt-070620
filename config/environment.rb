require 'bundler/setup'
Bundler.require(:default, :development)



# integration
#     should require bundler/setup in the environment (FAILED - 3)
#     should require the default and development groups in the environment (FAILED - 4)
#     should make the bundler gems available in bin/run.rb (FAILED - 5)